<UserControl x:Class="OpenWrtStudio.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="900">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="24">
            <!-- Header with Refresh -->
            <Grid Margin="0,0,0,18">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <StackPanel>
                    <TextBlock Text="Центр управления OpenWrt" FontSize="26" FontWeight="SemiBold" />
                    <TextBlock Text="Обзор аппаратных ресурсов, сетевых интерфейсов и быстрое управление" 
                               Foreground="{DynamicResource TextFillColorSecondaryBrush}" Margin="0,4,0,0" />
                </StackPanel>

                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <ui:Button Appearance="Secondary" Icon="{ui:SymbolIcon ArrowClockwise24}" Content="Обновить" 
                               Command="{Binding RefreshDashboardCommand}" />
                </StackPanel>
            </Grid>

            <!-- Quick Router Specs Banner -->
            <ui:Card Margin="0,0,0,16">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <ui:SymbolIcon Symbol="Router24" FontSize="44" Foreground="{DynamicResource AccentTextFillColorPrimaryBrush}" Margin="0,0,20,0" />

                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                        <TextBlock Text="{Binding Info.Model}" FontSize="18" FontWeight="SemiBold" />
                        <WrapPanel Margin="0,4,0,0">
                            <TextBlock Text="Прошивка: " Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
                            <TextBlock Text="{Binding Info.OpenWrtRelease}" FontWeight="Medium" Margin="0,0,16,0" />

                            <TextBlock Text="Ядро: " Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
                            <TextBlock Text="{Binding Info.KernelRelease}" FontWeight="Medium" Margin="0,0,16,0" />

                            <TextBlock Text="Хостнейм: " Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
                            <TextBlock Text="{Binding Info.Hostname}" FontWeight="Medium" />
                        </WrapPanel>
                    </StackPanel>

                    <StackPanel Grid.Column="2" VerticalAlignment="Center" HorizontalAlignment="Right">
                        <TextBlock Text="Аптайм" Foreground="{DynamicResource TextFillColorSecondaryBrush}" HorizontalAlignment="Right" />
                        <TextBlock Text="{Binding Info.UptimeFormatted}" FontSize="16" FontWeight="Bold" Foreground="{DynamicResource AccentTextFillColorPrimaryBrush}" HorizontalAlignment="Right" />
                    </StackPanel>
                </Grid>
            </ui:Card>

            <!-- Status Banner if Action Performed -->
            <ui:InfoBar Title="Статус операции" Message="{Binding ActionStatus}" 
                        IsOpen="{Binding ActionStatus, Converter={StaticResource BooleanToVisibilityConverter}}" 
                        Severity="Informational" Margin="0,0,0,16" />

            <!-- Metrics Cards Grid -->
            <UniformGrid Columns="3" Margin="0,0,0,16">
                <!-- CPU Load -->
                <ui:Card Margin="0,0,10,0">
                    <StackPanel>
                        <Grid Margin="0,0,0,10">
                            <TextBlock Text="Загрузка процессора" FontWeight="SemiBold" />
                            <ui:SymbolIcon Symbol="DeveloperBoard24" HorizontalAlignment="Right" Foreground="{DynamicResource AccentTextFillColorPrimaryBrush}" />
                        </Grid>
                        <TextBlock Text="{Binding Info.CpuUsagePercent, StringFormat='{}{0}%'}" FontSize="32" FontWeight="Bold" Margin="0,0,0,10" />
                        <ProgressBar Value="{Binding Info.CpuUsagePercent, Mode=OneWay}" Maximum="100" Height="6" Margin="0,0,0,10" />
                        <TextBlock Text="{Binding Info.Load1, StringFormat='Load Avg: {0:F2}'}" FontSize="12" Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
                    </StackPanel>
                </ui:Card>

                <!-- RAM Memory -->
                <ui:Card Margin="5,0,5,0">
                    <StackPanel>
                        <Grid Margin="0,0,0,10">
                            <TextBlock Text="Оперативная память (RAM)" FontWeight="SemiBold" />
                            <ui:SymbolIcon Symbol="Storage24" HorizontalAlignment="Right" Foreground="{DynamicResource AccentTextFillColorPrimaryBrush}" />
                        </Grid>
                        <TextBlock Text="{Binding Info.RamUsagePercent, StringFormat='{}{0}%'}" FontSize="32" FontWeight="Bold" Margin="0,0,0,10" />
                        <ProgressBar Value="{Binding Info.RamUsagePercent, Mode=OneWay}" Maximum="100" Height="6" Margin="0,0,0,10" />
                        <TextBlock Text="{Binding Info.RamFormatted}" FontSize="12" Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
                    </StackPanel>
                </ui:Card>

                <!-- Storage / Overlay -->
                <ui:Card Margin="10,0,0,0">
                    <StackPanel>
                        <Grid Margin="0,0,0,10">
                            <TextBlock Text="Хранилище (/overlay)" FontWeight="SemiBold" />
                            <ui:SymbolIcon Symbol="HardDrive24" HorizontalAlignment="Right" Foreground="{DynamicResource AccentTextFillColorPrimaryBrush}" />
                        </Grid>
                        <TextBlock Text="{Binding Info.OverlayUsagePercent, StringFormat='{}{0}%'}" FontSize="32" FontWeight="Bold" Margin="0,0,0,10" />
                        <ProgressBar Value="{Binding Info.OverlayUsagePercent, Mode=OneWay}" Maximum="100" Height="6" Margin="0,0,0,10" />
                        <TextBlock Text="{Binding Info.OverlayFormatted}" FontSize="12" Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
                    </StackPanel>
                </ui:Card>
            </UniformGrid>

            <!-- Quick Action Bar -->
            <ui:Card Margin="0,0,0,16">
                <StackPanel>
                    <TextBlock Text="Быстрые системные действия" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,12" />
                    <WrapPanel>
                        <ui:Button Margin="0,0,10,10" Appearance="Danger" Icon="{ui:SymbolIcon Power24}" Content="Перезагрузить роутер" Command="{Binding RebootCommand}" />
                        <ui:Button Margin="0,0,10,10" Appearance="Secondary" Icon="{ui:SymbolIcon ArrowSync24}" Content="Перезапустить сеть" Command="{Binding RestartNetworkCommand}" />
                        <ui:Button Margin="0,0,10,10" Appearance="Secondary" Icon="{ui:SymbolIcon Globe24}" Content="Перезапустить DNS" Command="{Binding RestartDnsCommand}" />
                        <ui:Button Margin="0,0,10,10" Appearance="Secondary" Icon="{ui:SymbolIcon Broom24}" Content="Очистить кэш DNS" Command="{Binding FlushDnsCacheCommand}" />
                    </WrapPanel>
                </StackPanel>
            </ui:Card>

            <!-- Network Interfaces Table -->
            <ui:Card>
                <StackPanel>
                    <TextBlock Text="Сетевые интерфейсы" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,12" />
                    <DataGrid ItemsSource="{Binding Interfaces}" AutoGenerateColumns="False" 
                              CanUserAddRows="False" IsReadOnly="True" 
                              HeadersVisibility="Column" GridLinesVisibility="Horizontal"
                              RowHeight="36" BorderThickness="0" Background="Transparent">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Интерфейс" Binding="{Binding Name}" Width="120" FontWeight="SemiBold" />
                            <DataGridTextColumn Header="Устройство" Binding="{Binding Device}" Width="110" />
                            <DataGridTextColumn Header="IP-адрес" Binding="{Binding IpAddress}" Width="160" />
                            <DataGridTextColumn Header="Шлюз" Binding="{Binding Gateway}" Width="140" />
                            <DataGridTextColumn Header="Получено (RX)" Binding="{Binding RxFormatted}" Width="130" />
                            <DataGridTextColumn Header="Отправлено (TX)" Binding="{Binding TxFormatted}" Width="130" />
                        </DataGrid.Columns>
                    </DataGrid>
                </StackPanel>
            </ui:Card>
        </StackPanel>
    </ScrollViewer>
</UserControl>
